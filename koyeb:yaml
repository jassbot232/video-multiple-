app: telegram-video-bot
services:
  - name: telegram-bot
    ports:
      - port: 8080
        protocol: HTTP
    scalings:
      min: 1
      max: 1
    env:
      - name: BOT_TOKEN
        secret: true
    docker:
      image: koyeb/telegram-video-bot
